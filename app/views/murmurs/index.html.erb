<div class="container">
  <div class="row">
    <div class="span6 offset3">
      <%= render 'form' %>
      <div id="murmurs-list">
        <% @murmurs.each do |murmur| %>
          <article id="murmur-item-<%= murmur.id %>" class="murmur-item"><%= link_to murmur.content, murmur.generated_url %></article>
        <% end %>
      </div>
    </div>
  </div>
</div>